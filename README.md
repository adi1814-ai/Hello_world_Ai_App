# Hello_world_Ai_App
this is a basic Streamlit dashboard demonstrating a simple linear regression model. It allows you to input
a value using a slider and outputs the model's prediction.

## Features
- Streamlit UI
- Simple scikit-learn regression model
- Interactive user model

## How to Run 
``` bash
pip install -r requirements.txt
streamlit run app.py
```